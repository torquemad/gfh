<h2>All Tags</h2>

<%= link_to 'new tag', new_tag_path %>

<% @tags.each do |tag| %>

  <p>
    <%= link_to tag.name, tag %> <br> 
    <%= link_to 'edit', edit_tag_path(tag) %>

    <%= form_for tag, method: :delete do |f| %>
      <%= f.submit 'Delete' %>
    <% end %>

  </p>

<% end %>